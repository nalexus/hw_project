# Reject the least-supported 1% of known OOF texts before confidence selection.
support_tail_rate: 0.01

# Maximum observed error among accepted OOF known-class predictions.
target_accepted_error: 0.02

# Minimum fraction of correctly classified OOF known texts retained by the policy.
minimum_correct_coverage: 0.95
